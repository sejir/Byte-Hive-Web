<form method="post" action="{{ path('app_reclamation_guide_delete', {'id': reclamation_guide.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation_guide.id) }}">
    <div class="grid">
    <button class="btn btn-primary btn-xl ">Delete</button>
    </div>
</form>
