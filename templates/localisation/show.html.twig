{% extends 'chiheb.html.twig' %}

{% block title %}Localisation{% endblock %}

{% block body %}
    <h1>Localisation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdEmplacement</th>
                <td>{{ localisation.idEmplacement }}</td>
            </tr>
            <tr>
                <th>NomEmplacement</th>
                <td>{{ localisation.nomEmplacement }}</td>
            </tr>
            <tr>
                <th>DEmplacement</th>
                <td>{{ localisation.dEmplacement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('localisation_index') }}">back to list</a>

    <a href="{{ path('localisation_edit', {'idEmplacement': localisation.idEmplacement}) }}">edit</a>

    {{ include('localisation/_delete_form.html.twig') }}
{% endblock %}
