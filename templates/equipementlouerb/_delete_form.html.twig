<form method="post" action="{{ path('app_equipementlouerb_delete', {'idequipement': equipementlouer.idequipement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipementlouer.idequipement) }}">
    <button class="btn btn-primary">Supprimer</button>
</form>
