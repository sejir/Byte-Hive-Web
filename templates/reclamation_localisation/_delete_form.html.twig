<form method="post" action="{{ path('app_reclamation_localisation_delete', {'id': reclamation_localisation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation_localisation.id) }}">
    <div class="grid">
    <button class="btn btn-primary btn-xl ">Delete</button>
    </div>
</form>
