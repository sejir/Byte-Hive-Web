<form method="post" action="{{ path('app_res_act_delete', {'idres': res_act.idres}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ res_act.idres) }}">
    <button class="btn">Delete</button>
</form>
