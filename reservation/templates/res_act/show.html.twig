{% extends 'Backend.html.twig' %}

{% block title %}ResAct{% endblock %}

{% block body %}
    <h1>ResAct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idres</th>
                <td>{{ res_act.idres }}</td>
            </tr>
            <tr>
                <th>Nomclient</th>
                <td>{{ res_act.nomclient }}</td>
            </tr>
            <tr>
                <th>Prenomc</th>
                <td>{{ res_act.prenomc }}</td>
            </tr>
            <tr>
                <th>Idact</th>
                <td>{{ res_act.idact }}</td>
            </tr>
            <tr>
                <th>NbrePerso</th>
                <td>{{ res_act.nbrePerso }}</td>
            </tr>
            <tr>
                <th>Numc</th>
                <td>{{ res_act.numc }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_res_act_index') }}">back to list</a>

    <a href="{{ path('app_res_act_edit', {'idres': res_act.idres}) }}">edit</a>

    {{ include('res_act/_delete_form.html.twig') }}
{% endblock %}
