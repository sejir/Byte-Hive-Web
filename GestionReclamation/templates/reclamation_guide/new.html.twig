{% extends 'basefront.html.twig' %}

{% block title %}New guide report{% endblock %}

{% block body %}
<!-- Favicon-->
        <link rel="icon" type="image/x-icon" href="assets/favicon.ico" />
        <!-- Bootstrap Icons-->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet" />
        <!-- Google fonts-->
        <link href="https://fonts.googleapis.com/css?family=Merriweather+Sans:400,700" rel="stylesheet" />
        <link href="https://fonts.googleapis.com/css?family=Merriweather:400,300,300italic,400italic,700,700italic" rel="stylesheet" type="text/css" />
        <!-- SimpleLightbox plugin CSS-->
        <link href="https://cdnjs.cloudflare.com/ajax/libs/SimpleLightbox/2.1.0/simpleLightbox.min.css" rel="stylesheet" />
        <!-- Core theme CSS (includes Bootstrap)-->
        <link href="css/styles.css" rel="stylesheet" />
    <!-- Report-->
        <section class="page-section" id="report">
            <div class="container px-4 px-lg-5">
                <div class="row gx-4 gx-lg-5 justify-content-center">
                    <div class="col-lg-8 col-xl-6 text-center">
                        <h2 class="mt-0">Report</h2>
                        <hr class="divider" />
                        <p class="text-muted mb-5">Ready to report a guide or location? Send us a message and we will get back to you as soon as possible!</p>
                    </div>
                </div>
            {# {{form(form)}} #}
            {{form_start(form)}}
            <div class="row gx-4 gx-lg-5 justify-content-center">
                <div class="col-lg-8 col-xl-6 text-center">
            <table>
            <tr>
                <td> {{form_label(form.fullname, "Full name")}} </td>
                <td> {{form_widget(form.fullname)}} </td>
                <td> {{form_errors(form.fullname)}} </td>
            </tr>
            <tr>
                <td> {{form_label(form.email, "Email")}} </td>
                <td> {{form_widget(form.email)}} </td>
                <td> {{form_errors(form.email)}} </td>
            </tr>
            <tr>
                <td> {{form_label(form.nom_guide, "Guide name")}} </td>
                <td> {{form_widget(form.nom_guide)}} </td>
                <td> {{form_errors(form.nom_guide)}} </td>
            </tr>
            <tr>
                <td> {{form_label(form.description, "Message")}} </td>
                <td> {{form_widget(form.description)}} </td>
                <td> {{form_errors(form.description)}} </td>
            </tr>
            </table>
            </div>
            </div>
            <!-- Submit success message-->
            <!---->
            <!-- This is what your users will see when the form-->
            <!-- has successfully submitted-->
                            <div class="d-none" id="submitSuccessMessage">
                                <div class="text-center mb-3">
                                    <div class="fw-bolder">Report added succesfully!</div>
                                    <br />
                                </div>
                            </div>
                            <!-- Submit error message-->
                            <!---->
                            <!-- This is what your users will see when there is-->
                            <!-- an error submitting the form-->
                            <div class="d-none" id="submitErrorMessage"><div class="text-center text-danger mb-3">Error sending message!</div></div>
                            <!-- Submit Button-->
                            <div class="row gx-4 gx-lg-5 justify-content-center">
                            <div class="col-lg-8 col-xl-6 text-center">
                            +++<form method="post" action="{{ path('app_reclamation_guide_new') }}" onsubmit="Report Added Successfully!">
                            
                            <div class="d-grid">
                            <button class="btn btn-danger btn-xl ">{{ button_label|default('Submit') }}</button> 
                            <a class="btn btn-primary btn-xl " href="{{ path('app_reclamation_guide_index') }}">Back to list</a>
                            </div>
                            </div>
                        {{form_end(form)}}
                        </form>
                    </div>
                </div>
                <div class="row gx-4 gx-lg-5 justify-content-center">
                    <div class="col-lg-4 text-center mb-5 mb-lg-0">
                        <i class="bi-phone fs-2 mb-3 text-muted"></i>
                        <div> </div>
                    </div>
                </div>
            </div>
        </section>
{% endblock %}
