{% extends 'baseb.html.twig' %}

{% block title %}Vendre{% endblock %}

{% block body %}
    <h1>Vendre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idequipement</th>
                <td>{{ vendre.idequipement }}</td>
            </tr>
            <tr>
                <th>Prixequipement</th>
                <td>{{ vendre.prixequipement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vendre_index') }}">back to list</a>

    <a href="{{ path('app_vendre_edit', {'idequipement': vendre.idequipement}) }}">edit</a>

    {{ include('vendre/_delete_form.html.twig') }}
{% endblock %}
